//Edit existing item or delete if amount left is 0

function editObject(var item, var newAmount)
{
  //go into item set amount to new amount

  //if item.amount == 0, delete item data

  return item;
}
