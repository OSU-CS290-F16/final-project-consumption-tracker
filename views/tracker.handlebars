<section class="history">
  <h2>{{tracker.name}}</h2>
  <div class="history-body">
    {{#each history}}
      {{>itemHistory unit=../tracker.unit}}
    {{/each}}
  </div>
</section>
{{>add-history-modal}}
